const player = (sign) => {
    this.sign = sign;

    const getSign = () => {
        return sign;
    };

    return {getSign};
};

const gameBoard = (() => {

})();

const gameController = (() => {

})();

const screenController = (() => {

})();